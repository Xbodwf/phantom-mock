version: '3.8'

services:
  fake-ai-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fake-ai-server
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=7143
